<div class="result-tile col-md-4 tile--bordered">
  <div class="result-tile__image col-md-4">
    <img src="{{result.volumeInfo.imageLinks.thumbnail}}" alt="{{result.volumeInfo.title}}" />
  </div>
  <div class="result-tile__header">
    <h3>{{result.volumeInfo.title}}{{#if result.volumeInfo.subtitle}}: {{result.volumeInfo.subtitle}}{{/if}}
      <div class="result-tile__buttons">
        <button type="button" class="btn btn-default btn-lg result-tile__buttons--styled" aria-label="Heart">
          <span class="glyphicon glyphicon-heart" aria-hidden="true"></span>
        </button>
      </div>
      <br>
    {{#each result.volumeInfo.authors as |author|}}
      <small>by <b>{{author}}</b></small>
    {{/each}}
    </h3>
  </div>
  <div class="result-tile__body">
    <div class="result-tile__ratings">

    </div>
    <div class="result-tile__description">
      {{#if result.volumeInfo.description}}
        <p>{{result.volumeInfo.description}}</p>
        {{else}}
        <p><i>There doesn't seem to be a description for this title just yet. This would be a great opportunity to read and find out what this book is about!</i></p>
      {{/if}}
    </div>
  </div>
</div>
