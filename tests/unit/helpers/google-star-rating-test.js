import { googleStarRating } from 'book-reviews/helpers/google-star-rating';
import { module, test } from 'qunit';

module('Unit | Helper | google star rating');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = googleStarRating([42]);
  assert.ok(result);
});
